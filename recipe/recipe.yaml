context:
  version: "1.0.0"
  python_min: "3.12"

package:
  name: mvlogics
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/m/mvlogics/mvlogics-${{ version }}.tar.gz
  sha256: 6f7be2fa64d78733002d88d5fe6a289f4d66101773ee303725100fb1cb4c8015

build:
  script:
    - uv pip install --python ${{ PYTHON }} --no-deps -vv .
  number: 0
  noarch: python

requirements:
  host:
    - python ${{ python_min }}.*
    - uv
  run:
    - python >=${{ python_min }}

tests:
  - python:
      python_version:
        - ${{ python_min }}.*
        - "*"
      imports:
        - mvlogics
      pip_check: true

about:
  homepage: https://github.com/jonathandung/mvlogics
  summary: 'Structured and well-equipped collection of unconventional systems logics with more than two truth values.'
  license: MIT
  license_file: LICENSE
  repository: https://github.com/jonathandung/mvlogics

extra:
  recipe-maintainers:
    - jonathandung
